[package]
name = "irongraph-embedding"
version = "0.1.4"
edition = "2024"
rust-version = "1.94"
publish = false

[features]
default = ["accelerator"]
# Automatic local text-embedding runtime.
accelerator = ["dep:candle-core", "dep:candle-nn", "dep:candle-transformers", "dep:irongraph-artifact-loader", "dep:tokenizers", "dep:candle-metal-kernels", "dep:objc2", "dep:objc2-foundation", "dep:objc2-metal", "dep:coreml-native", "irongraph-graph/accelerator", "irongraph-gpu/accelerator"]
metal = ["accelerator"]
cuda = ["accelerator", "candle-core/cuda", "candle-nn/cuda", "dep:cudarc", "irongraph-gpu/cuda"]

[dependencies]
irongraph-types = { path = "../types" }
irongraph-execution = { path = "../execution" }
irongraph-graph = { path = "../graph" }
irongraph-gpu = { path = "../gpu", default-features = false }
blake3 = { workspace = true }
libc = { workspace = true }
memmap2 = { workspace = true }
parking_lot = { version = "0.12.5", features = ["serde"] }
rustls = { version = "0.23.35", default-features = false, features = ["aws_lc_rs", "std"] }
serde = { version = "1.0.228", features = ["derive", "rc"] }
serde_json = "1.0.149"
sha2 = { workspace = true }
tokio-util = { version = "0.7.18", features = ["codec", "io", "rt"] }
tracing = "0.1.44"
url = "2.5.8"
webpki-roots = "1.0.6"
irongraph-artifact-loader = { path = "../artifact-loader", optional = true }

candle-transformers = { version = "0.11.0", optional = true }
tokenizers = { version = "0.22.2", optional = true, default-features = false, features = ["fancy-regex", "onig", "progressbar"] }

[target.'cfg(any(target_os = "macos", target_os = "ios"))'.dependencies]
candle-core = { workspace = true, optional = true, features = ["metal"] }
candle-metal-kernels = { version = "0.11.0", optional = true }
candle-nn = { workspace = true, optional = true, features = ["metal"] }
objc2 = { version = "0.6.3", optional = true }
objc2-foundation = { version = "0.3.2", optional = true, features = ["NSArray", "NSError", "NSString"] }
objc2-metal = { version = "0.3.2", optional = true }
coreml-native = { version = "0.2", optional = true }

[target.'cfg(not(any(target_os = "macos", target_os = "ios")))'.dependencies]
candle-core = { workspace = true, optional = true }
candle-nn = { workspace = true, optional = true }
cudarc = { version = "0.19.8", optional = true, default-features = false, features = ["cuda-version-from-build-system", "dynamic-loading", "fallback-latest", "nvrtc"] }

[dev-dependencies]
tempfile = "3.24.0"
